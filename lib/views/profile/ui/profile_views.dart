import 'package:flutter/material.dart';

class PerfoileView extends StatelessWidget {
  const PerfoileView({super.key});

  @override
  Widget build(BuildContext context) {
    return  Column(children: [
      // Add your Perfoile view widgets here
      Text('Perfoile View '),
      
    ],);
  }
}